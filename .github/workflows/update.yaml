name: "Update Upstream"

on:
  schedule:
    - cron: "5 4 * * *"
  workflow_dispatch:

permissions:
  id-token: write
  attestations: write

jobs:
  build:
    uses: wasilibs/actions/.github/workflows/update-node.yaml@77762dfec6f9722d1b57acdfe352dcd16a1dc272
    with:
      packages: sql-formatter
    secrets: inherit
